fastapi
uvicorn[standard]
sqlalchemy
alembic
pytest
psycopg[binary]
pydantic-settings
email-validator
numpy
pillow
tensorflow
python-jose[cryptography]
passlib[bcrypt]
# passlib 1.7.4 is incompatible with bcrypt 4.x — the backend probe tries
# to detect a wrap bug by hashing a 73-byte test secret which bcrypt 4.x
# rejects with "password cannot be longer than 72 bytes". Pin to bcrypt 3.x
# until the project migrates away from passlib.
bcrypt>=3.2,<4.0
python-multipart
httpx
slowapi
respx
gunicorn
